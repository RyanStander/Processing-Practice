 float a = 0;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     a = a + 0.1f;

     println( "The value of a is " + a );

println( "The value of a is 0 is " + ( a == 1 ) );
/* the computer does not understand recursion value and it runs out of space
for the full value which result in a floating point rounding error
*/
